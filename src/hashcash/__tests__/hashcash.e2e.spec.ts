/**
 * Phase 5 Wave 0 stub — HCSH-06 full cycle: 429 → solve (8-bit) → 200 → replay rejected.
 * Filled in by 05-06-PLAN.md.
 */
describe('Hashcash e2e', () => {
  it.todo('full cycle: high-risk request returns 429 with X-Hashcash-Challenge; solving at 8 bits and resubmitting returns 200; replaying same solution returns 429');
});

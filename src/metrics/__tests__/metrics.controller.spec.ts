/**
 * Phase 9 Wave 0 RED stubs for MetricsController (MTRC-03). Implementation lands in Plan 09-02.
 */
describe('MetricsController', () => {
  describe('GET /metrics (MTRC-03)', () => {
    it.todo('returns 200 with body from MetricsService.getAggregatedMetrics()');
    it.todo('sets Content-Type: text/plain; charset=utf-8');
    it.todo('class/method has @Public() metadata so JwtAuthGuard skips it');
  });
});
